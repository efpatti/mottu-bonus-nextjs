export interface ServiceOption {
  label: string;
  value: string;
  points: number;
}

export interface CalculatorEntry {
  id: string;
  type: string;
  count: number;
  points: number;
}
